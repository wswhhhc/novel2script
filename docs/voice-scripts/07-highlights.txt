Novel2Script 的技术亮点包括：

一，五阶段 AI 生成链路，确保角色一致性和结构完整性。

二，三百三十二行的严格 JSON Schema，涵盖 ID 格式、类型约束、引用完整性。

三，双模式架构，Mock 模式用于演示，AI 模式用于实际生成。

四，完整的前后端分离架构，FastAPI、React、TypeScript。

五，支持 DeepSeek、GPT、Claude 等多种 AI 模型。
